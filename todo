1. display empty cells with no data (when we have less data like only 1 record)
2. adjust width of some columns
3. Show success type message when completes
   else show error message.
4. When selects source, display a button that show a circular chart with size like a file manager
5. improve look and feel of table
6. file total size in table not lgtm. check it
7. add a sorting filter on table's columns
8. Add open folder button in table row to open the folder.